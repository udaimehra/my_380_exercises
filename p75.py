project_ids = {
    '01': 'open', 
    '03': 'in progress',
    '05': 'in progress',
    '04': 'completed'
}
result = list(set(project_ids.values()))
result.sort()
print(result)
#empty_list = []
#for i in list:
#    if i not in empty_list:
#        empty_list.append(i)
#empty_list.sort(key=None, reverse=False)
#print(empty_list)

